.row
  .col-lg-5.col-lg-offset-1
    .page-header
      %h1
        = user.fullname
        = user.online?

.row
  .col-lg-5.col-lg-offset-1
    .profile
      %i.fa.fa-user.fa-5x.fa-border.pull-left
      %h4
        %small First name:
        = user.firstname
      %h4
        %small Last name:
        = user.lastname
      - if is_himself || is_admin
        %h4
          %small Email:
          = user.email
      - else
        %br
    %br
    %hr.divider
    .joined
      %h5
        %small Joined:
        = user.joined
      %h5
        %small Last active:
        = user.last_active
      %h5
        %small Reviewed:
        = user.counted_reviews

    = render "users/products"

  .col-lg-5
    = render "users/reviews"
